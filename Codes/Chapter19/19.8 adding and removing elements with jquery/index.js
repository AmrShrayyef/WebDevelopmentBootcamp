$("h1").before("<button>new</button>"); /// adding a button before h1 element
$("h1").after("<button>new</button>");
$("h1").prepend("<button>new</button>"); //adding after the <h1>
$("h1").append("<button>new</button>");  // adding before the </h1> closing tag
$("button").remove(); // will remove all buttons in our webpage
